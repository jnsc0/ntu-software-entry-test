// Task 1: Declare an array that are going to be used to store patient's name
const patient_arr = [];

function addPatient(patientName){
    //Task 2: Add code here. Add code to add patient's name into the array declared in task 1
    patient_arr.push(patientName);
}

// Task 3: Add code here. Implement listPatient() function to print all patient's name stored in the array
function listPatient() {
    console.log(patient_arr);
}

addPatient("John"); 
addPatient("Mary");
addPatient("Mark");

listPatient();
